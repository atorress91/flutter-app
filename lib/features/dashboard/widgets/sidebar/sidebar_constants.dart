// Constantes para los anchos, fácil de modificar
const double sidebarExpandedWidth = 250;
const double sidebarCollapsedWidth = 80;

// Constantes de animación
const Duration sidebarAnimationDuration = Duration(milliseconds: 300);
const Duration headerAnimationDuration = Duration(milliseconds: 200);
const Duration navigationDelay = Duration(milliseconds: 100);

// Constantes de diseño
const double sidebarBorderWidth = 1;
const double menuItemBorderRadius = 12;
const double avatarRadiusCollapsed = 24;
const double avatarRadiusExpanded = 32;